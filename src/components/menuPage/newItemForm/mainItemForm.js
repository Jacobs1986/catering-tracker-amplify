import React from "react";

// CSS File
import "./newItemForm.css";

export default function MainItemForm() {
    return (
        <div>This is the form that will appear if the main item raido is checked.</div>
    );
};